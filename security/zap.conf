# ZAP baseline config example.
# You can set specific rules to FAIL/IGNORE/INFO.
# Keeping it minimal for demo; tune for your app.
# Format examples (rule-id	level	comment) depend on ZAP baseline script version.